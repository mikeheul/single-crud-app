<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product List</title>
</head>
<body>
    <h1>Products List</h1>
    <ul>
        <% products.forEach(product => { %>
            <li><a href="/api/products/<%= product.id %>"><%= product.nameUppercase %></a> - <%= product.price %> (<%= product.quantity %>) = <%= product.totalValue %></li>
        <% }) %>
    </ul>
</body>
</html>